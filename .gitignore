.DS_Store
__pycache__/
*.py[cod]
# Added by code-review-graph
.code-review-graph/

# Raw OCR — dead rules removed 2026-08-18. They named pre-recategorisation paths
# (Hora/UttaraKalamrita/...) that moved in 1cccca6, so they had matched nothing for weeks.
# Replaced by the repo-wide rule at the bottom: sources live in ../sanskrit-texts-sources/.

# Editor / agent tool-local config (not shared)
.claude/
.cursor/
.cursorrules
.kiro/
.mcp.json
.opencode.json
.windsurfrules

# Stray temp artifacts
temp_page.png
temp_*.png

# SOURCES LIVE IN ../sanskrit-texts-sources/ (DECISIONS 2026-07-17, enforced 2026-08-18).
# This repo holds the translation layer only: per-text JSON + README.md. Raw Devanagari
# transcriptions (.md), OCR dumps (.txt) and scans (.pdf) were moved out wholesale; these
# patterns stop them coming back by accident. README.md is deliberately NOT ignored.
*.pdf
*.txt
**/raw/
