.DS_Store
__pycache__/
*.py[cod]
# Added by code-review-graph
.code-review-graph/

# Local digitization intermediates (raw OCR dumps + section splits; JSON is the committed derived form)
Hora/UttaraKalamrita/raw_uttara_kalamrita.txt
Hora/UttaraKalamrita/sections/

# Editor / agent tool-local config (not shared)
.claude/
.cursor/
.cursorrules
.kiro/
.mcp.json
.opencode.json
.windsurfrules

# Stray temp artifacts
temp_page.png
temp_*.png

# Source scans — canonical data is the per-chapter JSON (DECISIONS 2026-07-17).
# PDFs stay LOCAL (144MB of .git); stripped from history in the bloat-rewrite step.
*.pdf
# Raw OCR working dirs
**/raw/
